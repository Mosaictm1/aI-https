# 📖 دليل المستخدم

## مقدمة

مرحباً بك في **AI-HTTP**! هذا الدليل سيساعدك على استخدام جميع مميزات التطبيق بكفاءة.

---

## 🚀 البداية

### تسجيل حساب جديد

1. افتح التطبيق على [https://ai-http.com](https://ai-http.com)
2. اضغط على **"إنشاء حساب"**
3. أدخل بياناتك:
   - البريد الإلكتروني
   - كلمة المرور (8 أحرف على الأقل)
   - الاسم
4. اضغط **"تسجيل"**
5. تحقق من بريدك الإلكتروني وفعّل حسابك

### تسجيل الدخول

1. أدخل البريد الإلكتروني وكلمة المرور
2. اضغط **"دخول"**

---

## 🔗 ربط n8n Instance

### الخطوة 1: الحصول على API Key من n8n

#### لمستخدمي n8n Cloud:
1. سجّل دخول على [n8n Cloud](https://app.n8n.cloud/)
2. اذهب إلى **⚙️ Settings** من القائمة الجانبية
3. اختر **API**
4. اضغط **"Create API Key"**
5. اختر الصلاحيات المطلوبة:
   - ✅ Read workflows
   - ✅ Write workflows
   - ✅ Execute workflows
6. انسخ الـ API Key

#### لمستخدمي n8n Self-hosted:
1. تأكد من تفعيل الـ API في إعدادات n8n
2. اتبع نفس الخطوات أعلاه

### الخطوة 2: إضافة Instance في AI-HTTP

1. من لوحة التحكم، اضغط **"+ إضافة Instance"**
2. أدخل:
   - **اسم الـ Instance**: اسم تعريفي (مثل: "Production n8n")
   - **URL**: رابط الـ n8n (مثل: `https://your-instance.n8n.cloud`)
   - **API Key**: المفتاح الذي نسخته
3. اضغط **"اختبار الاتصال"** للتحقق
4. اضغط **"حفظ"**

### إدارة Instances

- **عرض الكل**: من القائمة الجانبية > **Instances**
- **تعديل**: اضغط على ⚙️ بجانب الـ Instance
- **حذف**: اضغط على 🗑️ بجانب الـ Instance
- **تحديث**: اضغط على 🔄 لتحديث قائمة الـ Workflows

---

## 📋 إدارة Workflows

### عرض Workflows

1. اختر الـ Instance من القائمة المنسدلة
2. ستظهر قائمة بجميع الـ Workflows
3. استخدم البحث للعثور على workflow محدد
4. فلتر حسب:
   - **الحالة**: Active / Inactive
   - **التاريخ**: أحدث / أقدم
   - **الاسم**: أبجدياً

### معاينة Workflow

1. اضغط على اسم الـ Workflow
2. ستظهر:
   - **رسم بياني** للـ Workflow
   - **قائمة الـ Nodes**
   - **آخر تنفيذ**
   - **الإحصائيات**

### تشغيل Workflow

1. اختر الـ Workflow
2. اضغط **"▶️ تشغيل"**
3. (اختياري) أدخل بيانات الإدخال
4. راقب التنفيذ في الوقت الحقيقي

---

## 🔍 Workflow Debugger

### تشغيل Debug Mode

1. اختر الـ Workflow
2. اضغط **"🐛 Debug"**
3. أدخل بيانات الاختبار (إن وجدت)
4. اضغط **"بدء الـ Debug"**

### فهم واجهة الـ Debugger

```
┌─────────────────────────────────────────────────────────────┐
│  [Workflow Name]                              [⏱️ 2.3s]     │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│    ┌─────┐     ┌─────┐     ┌─────┐     ┌─────┐             │
│    │Start│ ──► │HTTP │ ──► │Code │ ──► │ End │             │
│    │ ✅  │     │ ❌  │     │ ⏸️  │     │ ⏸️  │             │
│    └─────┘     └─────┘     └─────┘     └─────┘             │
│                   │                                          │
│                   ▼                                          │
│         ┌─────────────────┐                                 │
│         │ Error: 401      │                                 │
│         │ Unauthorized    │                                 │
│         └─────────────────┘                                 │
│                                                              │
├─────────────────────────────────────────────────────────────┤
│  📊 Node Details          │  📝 Input/Output               │
│  ─────────────────────────│  ───────────────────────────── │
│  • Type: HTTP Request     │  Input:                        │
│  • Status: Failed         │  { "url": "https://..." }      │
│  • Duration: 1.2s         │                                 │
│  • Retries: 0             │  Output:                        │
│                           │  { "error": "401..." }          │
└─────────────────────────────────────────────────────────────┘
```

### الرموز

| الرمز | المعنى |
|-------|--------|
| ✅ | نجح التنفيذ |
| ❌ | فشل التنفيذ |
| ⏸️ | لم يُنفَّذ بعد |
| 🔄 | قيد التنفيذ |
| ⚠️ | تحذير |

### تحليل الـ Node

1. اضغط على أي Node
2. سترى:
   - **Input Data**: البيانات الداخلة
   - **Output Data**: البيانات الخارجة
   - **Configuration**: إعدادات الـ Node
   - **Error Details**: تفاصيل الخطأ (إن وجد)

---

## 🛠️ HTTP Node Builder

هذه هي الميزة الأقوى في AI-HTTP!

### الوصول للـ Builder

#### من Workflow:
1. افتح الـ Workflow
2. اضغط على HTTP Node
3. اضغط **"🛠️ فتح في Builder"**

#### مباشرة:
1. من القائمة الجانبية > **HTTP Builder**
2. اختر **"+ إنشاء Request جديد"**

### واجهة الـ Builder

```
┌─────────────────────────────────────────────────────────────┐
│  HTTP Request Builder                                        │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  Method:  [GET ▼]     URL: [________________________]       │
│                                                              │
│  ┌─────────────────────────────────────────────────────────┐│
│  │ Headers  │ Params  │ Body  │ Auth  │ Settings          ││
│  ├─────────────────────────────────────────────────────────┤│
│  │                                                          ││
│  │  Key                    Value                           ││
│  │  Content-Type           application/json                ││
│  │  Authorization          Bearer {{token}}                ││
│  │  + Add Header                                            ││
│  │                                                          ││
│  └─────────────────────────────────────────────────────────┘│
│                                                              │
│  [📥 Import from cURL]  [📤 Export]  [🧪 Test]  [💾 Save]  │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

### بناء Request خطوة بخطوة

#### 1. اختيار الـ Method
- **GET**: جلب بيانات
- **POST**: إرسال بيانات جديدة
- **PUT**: تحديث بيانات
- **PATCH**: تحديث جزئي
- **DELETE**: حذف بيانات

#### 2. إدخال الـ URL
```
https://api.example.com/v1/users
```

#### 3. إضافة Headers
| Header | مثال | الاستخدام |
|--------|-------|-----------|
| Content-Type | application/json | نوع البيانات |
| Authorization | Bearer token123 | المصادقة |
| Accept | application/json | نوع الاستجابة المطلوب |

#### 4. إضافة Query Parameters
```
?page=1&limit=10&sort=created_at
```

#### 5. إضافة Body (لـ POST/PUT/PATCH)
```json
{
  "name": "John Doe",
  "email": "john@example.com"
}
```

### استخدام القوالب

1. اضغط **"📚 Templates"**
2. اختر الخدمة (Stripe, Shopify, etc.)
3. اختر الـ Endpoint
4. سيتم ملء الإعدادات تلقائياً
5. عدّل القيم حسب حاجتك

### استيراد من cURL

1. اضغط **"📥 Import from cURL"**
2. الصق أمر cURL:
```bash
curl -X POST https://api.example.com/users \
  -H "Content-Type: application/json" \
  -d '{"name": "John"}'
```
3. اضغط **"تحويل"**
4. ستُملأ جميع الحقول تلقائياً

### اختبار الـ Request

1. اضغط **"🧪 Test"**
2. ستظهر نافذة الاختبار
3. راجع:
   - **Status Code**: مثل 200, 401, 404
   - **Response Headers**
   - **Response Body**
   - **Time Taken**

### حفظ للـ n8n

1. تأكد من نجاح الاختبار
2. اضغط **"💾 Save to n8n"**
3. اختر:
   - **Workflow**: أي workflow
   - **Node**: أي HTTP node (أو أنشئ جديد)
4. اضغط **"حفظ"**

---

## 🤖 تحليل الأخطاء بالذكاء الاصطناعي

### كيف يعمل

عند فشل HTTP Request:
1. يُحلَّل الخطأ تلقائياً
2. يُقارَن مع توثيق الخدمة
3. تُقترَح الحلول

### استخدام الميزة

1. عند ظهور خطأ، اضغط **"🤖 تحليل بالذكاء الاصطناعي"**
2. انتظر التحليل (3-10 ثواني)
3. ستظهر:

```
┌─────────────────────────────────────────────────────────────┐
│  🤖 AI Analysis                                              │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  ❌ المشكلة:                                                │
│  خطأ 401 - غير مصرح بالوصول                                 │
│                                                              │
│  🔍 السبب المحتمل:                                          │
│  الـ API Key منتهي الصلاحية أو غير صحيح                     │
│                                                              │
│  ✅ الحل المقترح:                                            │
│  1. تحقق من صحة الـ API Key                                 │
│  2. تأكد من عدم انتهاء صلاحيته                              │
│  3. تأكد من الصلاحيات المطلوبة                              │
│                                                              │
│  📚 المرجع:                                                   │
│  https://docs.example.com/authentication                     │
│                                                              │
│  [🔧 تطبيق الإصلاح تلقائياً]  [📋 نسخ الحل]                │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

### تطبيق الإصلاح تلقائياً

1. اضغط **"🔧 تطبيق الإصلاح"**
2. راجع التغييرات المقترحة
3. اضغط **"تأكيد"**
4. سيُحدَّث الـ Node في n8n

---

## 🧪 Test Console

### إنشاء اختبار جديد

1. من القائمة > **Test Console**
2. اضغط **"+ اختبار جديد"**
3. اختر:
   - **من Builder**: استخدم Request موجود
   - **من Workflow**: نسخ من HTTP Node
   - **جديد**: ابدأ من الصفر

### تشغيل الاختبارات

1. اختر الاختبارات
2. اضغط **"▶️ تشغيل"**
3. راقب النتائج

### مقارنة النتائج

```
┌────────────────────────────────────────────────────────────┐
│  Test Results Comparison                                    │
├────────────────────────────────────────────────────────────┤
│                                                             │
│  Test 1          Test 2           Diff                     │
│  ────────────    ────────────     ────────────             │
│  Status: 200     Status: 200      ✅ Match                 │
│  Time: 245ms     Time: 312ms      ⚠️ +27%                  │
│  Size: 1.2KB     Size: 1.2KB      ✅ Match                 │
│                                                             │
│  Body Diff:                                                 │
│  - "count": 10                                              │
│  + "count": 12                                              │
│                                                             │
└────────────────────────────────────────────────────────────┘
```

---

## ⚙️ الإعدادات

### إعدادات الحساب

- **تغيير كلمة المرور**
- **تفعيل المصادقة الثنائية (2FA)**
- **إدارة API Keys**
- **تفضيلات الإشعارات**

### إعدادات التطبيق

- **المظهر**: فاتح / داكن / تلقائي
- **اللغة**: العربية / English
- **المنطقة الزمنية**
- **تنسيق التاريخ**

---

## ⌨️ اختصارات لوحة المفاتيح

| الاختصار | الوظيفة |
|----------|---------|
| `Ctrl + S` | حفظ |
| `Ctrl + Enter` | تشغيل الاختبار |
| `Ctrl + Shift + P` | Command Palette |
| `F5` | تحديث |
| `Escape` | إغلاق النافذة المنبثقة |
| `Ctrl + /` | عرض الاختصارات |

---

## 💡 نصائح

1. **استخدم القوالب**: توفر وقت كبير
2. **احفظ Requests ناجحة**: لإعادة استخدامها
3. **راجع التوثيق**: الروابط متوفرة دائماً
4. **استخدم AI Analysis**: حتى للأخطاء البسيطة
5. **نظّم Workflows**: أسماء واضحة وتصنيفات

---

## 📞 الدعم

- **الأسئلة الشائعة**: [FAQ](./faq.md)
- **Discord**: [انضم لمجتمعنا](https://discord.gg/ai-http)
- **Email**: support@ai-http.com
